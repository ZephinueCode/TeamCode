/**
 * SQLite persistence — deferred.
 * Schema definitions are in schema.ts for migration when needed.
 * Currently TeamCode uses in-memory session storage.
 */
import * as s from "./schema"
export const schema = s
